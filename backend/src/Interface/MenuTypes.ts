export interface MenuItems {
    itemName?: string
    price?: number
    itemDescription?: string
}

export interface MenuTypes {
    menuName?: string
    description?: string
    items?: MenuItems[]
}

